var User = angular.module("User", []);

User.factory("User", ["$http", "$sessionStorage", "$location", "$window", function($http, $sessionStorage, $location, $window){
	var User = {}

    
    

	return User
}])
